<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".GameActivity">

    <!-- NOVA: Mão do Bot (Cartas viradas para baixo) -->
    <HorizontalScrollView
        android:id="@+id/botHandScrollView"
        android:layout_width="match_parent"
        android:layout_height="110dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="#CCCCCC"
        android:paddingVertical="4dp">

        <LinearLayout
            android:id="@+id/botHandLayout"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:padding="8dp"
            android:gravity="center_vertical"/>
    </HorizontalScrollView>

    <LinearLayout
        android:id="@+id/trumpLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/botHandScrollView"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="TRUNFO"
            android:textSize="12sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp"/>

        <TextView
            android:id="@+id/trumpCardView"
            android:layout_width="80dp"
            android:layout_height="120dp"
            android:gravity="center"
            android:textSize="18sp"
            android:background="#FFFFFF"
            tools:text="7\n♦"
            tools:textColor="#FF0000" />

        <TextView
            android:id="@+id/playerScoreView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Tu: 0" />

        <TextView
            android:id="@+id/botScoreView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Bot: 0" />

        <!-- Deck (Baralho) Container Vazio (Preenchido via Código) -->
        <FrameLayout
            android:id="@+id/deckContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp">
            <!-- O conteudo será adicionado dinamicamente pelo codigo -->
             <TextView
                android:id="@+id/deckView"
                android:layout_width="80dp"
                android:layout_height="120dp"
                android:background="@drawable/back_card_red"
                android:gravity="center"
                android:textColor="#000000"
                android:textSize="24sp"
                android:textStyle="bold"
                tools:text="30" />
        </FrameLayout>

    </LinearLayout>

    <!-- Carta jogada pelo Bot -->
    <TextView
        android:id="@+id/botPlayedCardView"
        android:layout_width="100dp"
        android:layout_height="150dp"
        android:gravity="center"
        android:textSize="20sp"
        android:background="#FFFFFF"
        android:visibility="invisible"
        app:layout_constraintTop_toBottomOf="@id/botHandScrollView"
        app:layout_constraintBottom_toTopOf="@id/playerPlayedCardView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="K\n♣"
        tools:visibility="visible" />

    <!-- Carta jogada pelo Jogador -->
    <TextView
        android:id="@+id/playerPlayedCardView"
        android:layout_width="100dp"
        android:layout_height="150dp"
        android:gravity="center"
        android:textSize="20sp"
        android:background="#FFFFFF"
        android:visibility="invisible"
        app:layout_constraintBottom_toTopOf="@id/playerHandScrollView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/botPlayedCardView"
        tools:text="A\n♥"
        tools:visibility="visible" />

    <!-- Mão do Jogador -->
    <HorizontalScrollView
        android:id="@+id/playerHandScrollView"
        android:layout_width="match_parent"
        android:layout_height="130dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="#E0E0E0"
        android:paddingVertical="4dp">

        <LinearLayout
            android:id="@+id/playerHandLayout"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:padding="8dp"
            android:gravity="center_vertical"/>
    </HorizontalScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>